{
  "name": "@bharatmcp/mcp-server",
  "version": "0.1.0",
  "description": "MCP server — tool registration, validation, and API execution engine.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@bharatmcp/types": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}